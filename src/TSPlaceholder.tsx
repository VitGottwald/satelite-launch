import * as React from 'react';

const centerStyle = {
  color: 'white',
  height: '100%',
  display: 'flex',
  'align-items': 'center',
  'text-align': 'center',
  'justify-content': 'center',
};

const Placeholder = () => (
  <div style={centerStyle}>
    Here belongs a Falcon9 with Rocket, Payload, and Fire
  </div>
);

export default Placeholder;
